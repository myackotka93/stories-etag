import React from 'react';
export const SpriteAlignCenter = props => (
  <svg viewBox="0 0 20 20" width="16px" height="16px" {...props} className={`etag-font ${props.className ? props.className : ''}`}></svg>
);
